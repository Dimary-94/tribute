{{ attach_library('tribute/hero-component') }}
<div class="hero-component" style="background-image: url('{{image.0}}')">
	<div class="container">
		<div class="hero-desc">
			<div class="hero-component__body">
				{{ body }}
			</div>

			<div class="hero-btn">
				{{ link }}
			</div>
		</div>
	</div>
</div>
